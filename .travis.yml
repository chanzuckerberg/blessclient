language: go
go:
- '1.11'
install:
  - npm install -g snyk
jobs:
  include:
    - stage: check
      script: make enforce-cover
    - stage: check
      script:
        - snyk monitor --org=czi
        - snyk test

after_success:
  - bash <(curl -s https://codecov.io/bash)
